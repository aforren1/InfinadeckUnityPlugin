using UnityEngine;

namespace Infinadeck.Samples.XRPlayground
{
    /**
     * Marker component. Coins collect themselves when a collider carrying this
     * component (a palm or a controller) enters their trigger.
     */
    public class CoinCollector : MonoBehaviour
    {
    }
}
